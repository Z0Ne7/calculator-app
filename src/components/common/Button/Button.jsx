import React from 'react';

const Button = (props) => <div class="flex h-16 justify-center items-center font-light text-2xl bg-gray-50 text-gray-500 flex-1">{props.children}</div>;

export default Button;
